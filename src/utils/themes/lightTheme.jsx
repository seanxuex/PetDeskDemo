import { createTheme } from '@mui/material/styles'

const lightTheme = createTheme({
  palette: {
    mode: 'light',
    main: {
      background: 'white',
      title: 'black',
    },
  },
})

export default lightTheme
